# Runtime
rumps>=0.4.0          # menu bar app (pulls in pyobjc-framework-Cocoa)

# Dev / packaging (macOS)
py2app>=0.28          # build Tomatick.app
pytest>=7.0           # run the logic tests

# Transitive floors. py2app and pytest accept very old setuptools/pygments, and a
# resolver taking the minimum satisfying version lands on setuptools 9.1.0 and
# pygments 2.9.0 — both carrying known CVEs. These pins raise the floor; they do
# not change what a current install resolves to.
setuptools>=83.0.0    # via py2app
pygments>=2.20.0      # via pytest
